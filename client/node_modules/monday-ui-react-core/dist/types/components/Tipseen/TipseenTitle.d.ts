import { FC } from "react";
import { VibeComponentProps } from "../../types";
interface TipseenTitleProps extends VibeComponentProps {
    text?: string;
}
declare const TipseenTitle: FC<TipseenTitleProps>;
export default TipseenTitle;
